import { createApp } from "vue";
import "./style.css";
import App from "./App.vue";

createApp(App).mount("#app");

import New from "./New.vue";

createApp(New).mount("#new");

import More from "./Rename.vue";

createApp(More).mount("#rename");
